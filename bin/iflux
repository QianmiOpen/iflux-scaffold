#!/usr/bin/env node

var program = require('commander');


program
  .version(require('../package.json').version)
  .command('startproject [project-name]', 'start new project')
  .command('startapp [app-name]', 'create new app')
  .command('run project', 'run server')
  .parse(process.argv);
